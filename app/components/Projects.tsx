import Image from 'next/image'
import { FiArrowUpRight } from 'react-icons/fi'
import type { Project } from '@/types'

export function Projects({ projects }: { projects: Project[] }) {
  return <section id="proyectos" className="border-t border-white/10 px-5 py-24 lg:px-8"><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="font-mono text-sm uppercase tracking-[.25em] text-cyan">Proyectos seleccionados</p><h2 className="mt-3 font-display text-3xl font-semibold text-white sm:text-5xl">Trabajo que habla <span className="gradient-text">por sí solo.</span></h2></div><span className="font-mono text-xs text-slate-500">{projects.length.toString().padStart(2, '0')} proyectos</span></div><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{projects.map((project) => <article key={project.title} className={`glass neon-border group overflow-hidden rounded-2xl transition duration-500 hover:-translate-y-2 ${project.featured ? 'md:col-span-2 lg:col-span-2' : ''}`}><div className="relative aspect-[16/9] overflow-hidden bg-surface"><Image src={project.image} alt={`Vista previa de ${project.title}`} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 66vw" /></div><div className="p-6"><div className="flex items-start justify-between gap-4"><h3 className="font-display text-xl font-semibold text-white">{project.title}</h3><a href={project.href} target="_blank" rel="noreferrer" aria-label={`Ver proyecto ${project.title}`} className="rounded-full border border-white/15 p-2 text-slate-300 transition hover:border-cyan hover:text-cyan"><FiArrowUpRight /></a></div><p className="mt-3 text-sm leading-6 text-slate-400">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.technologies.map((tech) => <span key={tech} className="rounded-full bg-white/5 px-3 py-1 font-mono text-[11px] text-slate-400">{tech}</span>)}</div></div></article>)}</div></div></section>
}
